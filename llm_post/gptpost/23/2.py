```python
def strlen(string: str) -> int:
    """ Return length of given string
    >>> strlen('')
    0
    >>> strlen('abc')
    3
    """
    # Postcondition: The length of the returned string should be equal to the number of characters in the input string.
    assert len(string) == return_val
```